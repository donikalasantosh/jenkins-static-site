pipeline {
    agent any

    stages {
        stage('Clone Repo') {
            steps {
                git 'https://github.com/donikalasantosh/jenkins-static-site.git'
            }
        }
        stage('Deploy') {
            steps {
                sh 'scp -r * ubuntu@13.219.81.158:/var/www/html/'
            }
        }
    }
}
